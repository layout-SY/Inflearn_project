let val;

val = window.outerHeight; // 창 자체의 가로 길이
val = window.innerHeight; // body 부분 가로 길이

val = window.outerWidth;
val = window.innerWidth;

val = window.scrollY;
val = window.scrollX;

val = window.location;
// val = window.location.href = 'http://google.com';
val = window.location;

// val = window.history.forward();
// val = window.history.back();

val = window.navigator;

console.log(val);
